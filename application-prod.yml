dt:
  config:
    multitenant:
      tenant:
        supportedTenants:
          - pl
          - cz
          - ab
        tenantHeaders:
          - X-TENANT-ID
      feign:
        properties:
          defaults:
            enabled: true
            interval: 10
            name: different-service
eureka:
  instance:
    expire-duration: 5
